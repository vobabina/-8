#ifndef ARRAY_UTILS_H
#define ARRAY_UTILS_H

#include <string>

const int N = 20;

void get_mas(std::string filename, int arr[N], int &n);
void show_mas(std::string filename, int arr[N], int n);
void transform_array(int arr[N], int n);

#endif
